const size = {
  xs: '8px',
  sm: '16px',
  md: '32px',
  lg: '50px',
  xl: '300px',
  xxl: '500px',
}

const grayFonts = '#999999'
const cardBg = '#CCD1D1 '

const theme = {
  xs: `${size.xs}`,
  sm: `${size.sm}`,
  md: `${size.md}`,
  lg: `${size.lg}`,
  xl: `${size.xl}`,
  xxl: `${size.xxl}`,
  grayFonts,
  cardBg,
}

export { theme }
